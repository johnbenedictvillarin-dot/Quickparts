{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "mkdir -p /tmp/views /tmp/cache /tmp/sessions && composer install --no-dev --optimize-autoloader"
  },
  "start": {
    "web": "php artisan serve --host=0.0.0.0 --port=8080"
  }
}